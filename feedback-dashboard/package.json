{
  "name": "feedback-dashboard",
  "version": "1.0.0",
  "description": "Simple feedback aggregation dashboard",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}